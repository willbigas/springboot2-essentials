version: '3.1'
services:
  db:
      image: mysql
      restart: always
      environment:
        MYSQL_ROOT_PASSWORD: root
        MYSQL_DATABASE: devdojo
      ports:
        - "3307:3306"
      volumes:
      - devdojo_data:/var/lib/mysql

volumes:
  devdojo_data:

